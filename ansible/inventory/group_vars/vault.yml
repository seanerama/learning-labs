---
# Encrypted credentials (use ansible-vault to encrypt this file)
# Example: ansible-vault encrypt inventory/group_vars/vault.yml

# Network Device Credentials
vault_network_password: "cisco123"
vault_enable_password: "cisco123"

# SNMP Credentials
vault_snmp_community: "private"

# API Keys (if needed)
vault_api_key: "your-api-key-here"

# Note: In production, encrypt this file with:
# ansible-vault encrypt inventory/group_vars/vault.yml
