# Lab-specific dependencies
# Note: SimpleUI has its own requirements.txt - install those first
# These are additional dependencies needed for the lab's MCP servers

# Required for network_tools.py DNS lookups
dnspython>=2.4.0

# FastMCP for building MCP servers (also in SimpleUI, but listed for standalone testing)
fastmcp>=0.1.0
